=begin
The differences between arrays, and hashes:
An array is an object containing specific values, 
whereas a hash is an object that contains values,
correlating with other object. The values are 
always in pairs in a hash, however, there can be 
different values. The pairs can be a string paired 
with an integer, a symbol paired with a hash itself. 
Simply put, two seperate values paired in the hash, 
called the key, and the value
=end
